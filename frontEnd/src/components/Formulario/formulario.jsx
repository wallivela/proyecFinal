// Formulario.jsx
import React, { useState } from 'react';

const Formulario = ({ zona, onSubmit }) => {
  // ... tu código del formulario ...

  return (
    <div className="formulario-container">
      {/* ... contenido del formulario ... */}
    </div>
  );
};

export default Formulario;  // Asegúrate de tener esta línea para exportar por defecto
